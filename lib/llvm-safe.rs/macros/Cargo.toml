[package]
name = "macros"
edition = "2021"
version = "0.1.0"

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "fold"] }

[lib]
proc-macro = true

[features]
nightly = []
